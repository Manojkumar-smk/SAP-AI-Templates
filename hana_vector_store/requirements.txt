# Install all dependencies:
#   pip install -r requirements.txt

hdbcli==2.17.*            # HANA raw SQL connection
python-dotenv             # .env file loading
pandas                    # CSV / Excel loading
openpyxl                  # Excel .xlsx support

sap-ai-sdk-gen            # SAP Gen AI Hub proxy  →  pip install sap-ai-sdk-gen
langchain~=0.3.25
langchain-core~=0.3.25
langchain-community~=0.3.25
langchain-openai>=0.3.7

pypdf                     # PDF loading (load_from_pdf)
beautifulsoup4            # URL / web page loading (load_from_url)
requests                  # URL / web page loading (load_from_url)
