# Install all dependencies:
#   pip install -r requirements.txt

sap-ai-sdk-gen[all]       # SAP Cloud SDK for AI (Python) — includes orchestration module
python-dotenv             # .env file loading
